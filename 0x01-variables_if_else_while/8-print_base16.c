#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char word;

	for (word = '1'; word <= '9'; word++)
		putchar(word);
	for (word = 'a'; word <= 'f'; word++)
		putchar(word)
	putchar('\n');

	return (0);
}
